apiVersion: v1
kind: Pod
metadata:
  name: yolo-client
  labels:
    app: yolo
    layer: frontend
spec:
  containers:
  - image: reaganc/yolo-client:1.5
    name: yolo-client
    tty: true
    stdin: true
    ports:
    - containerPort: 3000
      protocol: TCP